import { Routes } from '@angular/router';
import { TaskSuggestComponent } from '../components/task-suggestion/task-suggestion';

export const routes: Routes = [{ path: '', component: TaskSuggestComponent }];
